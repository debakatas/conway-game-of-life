import React from 'react';
import styled from 'styled-components';

const Title = styled.h1`
    text-align: center;
    color: var(--dk-pink);
    margin: 2rem;
`;

const App = () => {
    return (
        <Title>
            🌵 deBakatas Ready!
        </Title>
    );
};

export default App;